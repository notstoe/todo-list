// shows tasks details

function expandTask(e){

    console.log(e.target.parentNode);

    // TODO - expand the task to show, full date (dd/mm/yyyy), description and title on top
    // make a class and hide the details with display: none, then just toggle class

}

export { expandTask }